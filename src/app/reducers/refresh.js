const refresh = (state = null, action) => {
    switch (action.type) {
        case "REFRESH": {
            return 
        }
        default:
            return state;
        }
  };
  
  export default refresh;
  